extends layout.pug

block content
    section#contentfooter
        h2 Welcome, #{name}!
        form(action='/goodbye', method='post')
            button(type='submit') Goodbye!
        p
            a(href='/cards') Begin!